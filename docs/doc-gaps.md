# Documentation gaps — `bsf-client`

## Co-Authored-By: Claude <noreply@anthropic.com>

This is the tracked, closeable list of **what the client docs still don't cover**. It is the client
analogue of `bsf-server/docs/doc-gaps.md` ([local](../../bsf-server/docs/doc-gaps.md) | [GitHub](https://github.com/Banner-Saga-Factions/BSF-Custom-Server/blob/main/bsf-server/docs/doc-gaps.md)).

## No open gaps

The documentation track (**P1 → P3**; PRs #15 and #16, plus the P3 pull request that carries this
change) closed every gap tracked here. The `docs/` suite now covers the patch/runtime model, the build
workflow, the wire protocol, the multiplayer battle engine, the UI and asset pipeline, the data model,
the offline AI, and the mod bridge. The full track plan is
[`../misc/Plan-Docs-Track-2026-07-02.md`](../misc/Plan-Docs-Track-2026-07-02.md).

**When a new gap appears:** add an entry below — the gap, why it matters, and the source material a
writer should mine — and **delete it when the closing doc lands** (don't strike it through). Keep this
list honest: an empty list means the durable docs are believed complete, not that nobody has looked.
